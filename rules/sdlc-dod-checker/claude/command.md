# Dod Checker (Claude Code)



## Instructions
When reviewing code or preparing a PR, implicitly check the changes against the project's Definition of Done.

**CRITICAL**: You MUST call the `get_definition_of_done` tool (e.g., with `component="feature"` or `component="release"`) to retrieve the exact DoD criteria for this project, and explicitly evaluate the changes against those criteria.
